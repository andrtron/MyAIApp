      - name: Build APK (с подробным выводом)
        run: |
          if [ -x "./gradlew" ]; then
            chmod +x ./gradlew
            ./gradlew clean assembleDebug --stacktrace --info --warning-mode all
          else
            gradle clean assembleDebug --stacktrace --info --warning-mode all
          fi
